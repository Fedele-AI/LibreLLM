# Core dependencies - works on CPU and CUDA
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
tokenizers>=0.14.0
einops>=0.7.0
tqdm>=4.65.0
numpy>=1.24.0
jinja2>=3.1.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional: Weights & Biases for experiment tracking
# Uncomment if you want to use wandb
# wandb>=0.15.0

# Note: We use a pure PyTorch implementation of Mamba-2
# No need for mamba-ssm or causal-conv1d (they require CUDA compilation)
